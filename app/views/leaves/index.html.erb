<div class="row">
  <div class="col-lg-12">
      <% if current_user && current_user.has_role?(:admin) %>
        <%= render "stats" %>
      <% elsif current_user && current_user.has_role?(:employee) %>
        <%= render "stats" %>
      <% end %>
      <section class="panel">
        <div class="panel-body">
          <% if current_user && current_user.has_role?(:admin) %>
              <%= render "filter" %>
          <% elsif current_user && current_user.has_role?(:employee) %>
              <%= render "filter" %>
          <% end %>
          <%= render 'list'%>  
        </div> 
      </section>
  </div>
</div>
